import React from 'react'

const Interview1 = () => {
  return (
    // we can send only one parent but if we use comma between them then we can return more than one parent but it renders the last one in APP.jsx file
    <div>
      First Element
    </div>,
    <div>Second ELement</div>
  )
}


export default Interview1 ;